# Ignore Gradle project-specific cache directory
.gradle
.idea
requirements.md
# Ignore Gradle build output directory
build

# SSL certificates (generated locally on each server)
ssl/*.p12
ssl/*.crt
ssl/*.key
ssl/*.pem
